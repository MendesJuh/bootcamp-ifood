// + (adição): Soma dois valores.
// - (subtração): Subtrai o segundo valor do primeiro.
// * (multiplicação): Multiplica dois valores.
// / (divisão): Divide o primeiro valor pelo segundo.
// % (módulo): Retorna o resto da divisão do primeiro valor pelo segundo.

// Operadores de Comparação:

// == (igual a): Compara se dois valores são iguais.
// != (diferente de): Compara se dois valores são diferentes.
// === (igual a estritamente): Compara se dois valores são iguais em valor e tipo.
// !== (diferente de estritamente): Compara se dois valores são diferentes em valor ou tipo.
// > (maior que), < (menor que), >= (maior ou igual a), <= (menor ou igual a): Comparam dois valores em relação à sua magnitude.

//Operadores Lógicos
// && (E lógico): Retorna verdadeiro se ambas as expressões forem verdadeiras.
// || (OU lógico): Retorna verdadeiro se pelo menos uma das expressões for verdadeira.
// ! (NÃO lógico): Inverte o valor de verdade de uma expressão.